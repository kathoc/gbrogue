#ifndef UI_LOG_H
#define UI_LOG_H

/* Full-screen message-log viewer (opened from the in-game menu). Shows
   the recent history, oldest at the top and newest at the bottom; any of
   A/B/START closes it. */
void ui_log_show(void);

#endif
